#
# Configuration file for modules
#
# syntax:    <MODULE> [=] <IMPLEMENTATION>
#
# Current operations with modules
#
# barrier-all = linear
# barrier = linear | tree
# broadcast = linear | tree
# fcollect = linear
# collect = linear

#
# Chosen for all routines if not explicitly given
#
# default     = linear
