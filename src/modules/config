# config file for modules
# syntax:    <MODULE> [=] <IMPLEMENTATION>

# chosen for all routines if not explicitly given
# default     = linear

# examples: override defaults
# barrier_all built on top of shmem primitives
# barrier-all = shmem
# barrier     = tree
# broadcast   = tree
# fcollect    = linear
# collect     = linear
