#
# common install defs.  should come from GNU configure at some point
#

#
# installation root
#
PREFIX           = $(HOME)/SHMEM

# ---------------------------------------------------------

GASNET_SEGMENT_STYLE = everything

#
# GASNet installation root, and setup
#
GASNET           = $(HOME)/gasnet/$(GASNET_SEGMENT_STYLE)
GASNET_CONDUIT   = mpi

include $(GASNET)/include/$(GASNET_CONDUIT)-conduit/$(GASNET_CONDUIT)-par.mak 
